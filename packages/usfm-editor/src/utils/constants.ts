// Base project location
export const PROJECT_BASE_LOCATION =
  '/home/samueljohn/.scribe/projects/lexical_test';

// Source and target project locations
export const SOURCE_PROJECT_LOCATION = `${PROJECT_BASE_LOCATION}/source`;
export const TARGET_PROJECT_LOCATION = `${PROJECT_BASE_LOCATION}/target`;

// For backward compatibility
export const PROJECT_LOCATION = TARGET_PROJECT_LOCATION;

// Standard book file extension
export const BOOK_FILE_EXTENSION = '.usj';

// Default book to open if no book is specified
export const DEFAULT_BOOK_ID = 'JHN';

export const SCOPE = ['PSA', 'GEN', 'JHN', 'HAB'];
